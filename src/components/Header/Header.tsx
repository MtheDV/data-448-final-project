const Header = () => {
  return (
    <header className={'flex items-center h-14 border-b border-b-gray-100 p-3 sticky top-0 z-50 bg-white'}>
      Header Details
    </header>
  );
};

export default Header;
